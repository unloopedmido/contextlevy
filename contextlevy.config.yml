# Dogfood config — explicit legacy mode preserves pre-v2.4 comment/cost defaults
mode: legacy
token-threshold: 500
comment-format: default
pricing-profiles:
  - name: GPT-5.5
    inputCostPerMillion: 5.0
  - name: Kimi K2.6
    inputCostPerMillion: 0.95
